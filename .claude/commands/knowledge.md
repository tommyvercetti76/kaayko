You are working on the **Knowledge module** of Kaayko (`/Users/Rohan/Kaayko_v6/kaayko`).

**Note:** This module is not yet in MODULE-MAP.md — it appears to be an in-progress or recently added ROOTS-adjacent knowledge engine.

## Pages
| URL | File |
|-----|------|
| `/knowledge` | `kaayko/src/knowledge/index.html` — knowledge engine landing (dark-mode UI) |
| `/knowledge/about` | `kaayko/src/knowledge/about.html` |
| `/knowledge/request` | `kaayko/src/knowledge/request.html` — request access form |

## What it is
Based on the UI and file structure, this appears to be a B2C-facing interface for the ROOTS knowledge/assessment platform — distinct from the CoolSchools deployment. It uses a dark-mode design, suggesting it's a separate brand or product surface.

## API files
Unknown — needs investigation. Likely uses the same `/api/v1/roots/*` or `/api/knowledge/*` backend.

## Task
$ARGUMENTS

**Before working on this module:** Read the actual HTML files to understand the current state, then check what API endpoints they call.
